/**
 * Single import point for the classifier.
 * Uses the real trained EfficientNetB0 Keras model adapter.
 */

import { classifyImage } from './trainedModelClassifier.js';

export { classifyImage };
